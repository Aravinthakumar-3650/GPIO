void setup() 
{
  volatile char *dira,*outa;
  volatile long i,j;
  dira=0x21;
  *dira=0xff;
  outa=0x22;
  
  while(1)
  {
   char seq[8]={ 0,2,1,3,4,6,5,7 } ;
   for(i=0;i<8;i++)
   {
    *outa=(1<< seq[i]);
    for(j=0;j<450000;j++);
   }
  }
}
void loop() {}
